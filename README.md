# Mini Snowflake

Mini Snowflake is a compact distributed SQL query engine prototype.

## Start

```bash
python -m mini_snowflake.cli
```

You can also pass SQL directly:

```bash
python -m mini_snowflake.cli --sql "select 1"
```

